# Aspect audits — 2026-07-02 (10 undertouched surfaces, 3 moves each)

Source: judged multi-agent audit. Shipped already: daily-mission (reskin+delta+voice), mentor-chat-alhassan (all 3), onboarding-landing (redesign). The rest feed ROADMAP items.

## zielplan
Zielplan (client/src/Zielplan.jsx) is a Phase-1 manual goal-planner overlay: the learner must invent a title, pick a deadline, then hand-add every day and every step before anything useful happens — and its home button is disabled in App.jsx (line 4410, `false && canStart`) precisely because that blank-slate meta-work was judged clutter, leaving it reachable only via the Brain 'apply' prescription. Its bones are strong (server-backed plans, AI step-runners, a real speaking recorder, mock-fight launching that marks steps done), but the cold start kills it, nothing surfaces TODAY, and the chrome is sub-premium (8.5–9.5px text, 20px checkboxes, off-palette reds).
- **Interview-Countdown: one tap from real interview date to a full auto-built plan** (M): Reframe the create card as 'ECHTES INTERVIEW GEPLANT?' with exactly two inputs: the interview date and an optional job title. On create, a pure client-side function buildCountdownDays(todayStr(), deadline) in Zielplan.jsx lays a proven prep arc backward from the date using the EXISTING plumbing (createPlan, then one putPlan({ days })): early days get research + written steps, middle days speaking steps, the final 1-2 days the mock-fight(s), each pre-seeded with topics from a hardcoded DE topic bank aimed at Arabic-L1 walls (e.g. 'Selbstvorstellung: 60-Sekunden-Pitch', 'Stärken mit konkretem Beispiel — Verb ans Ende', 'Gehaltsfrage kontern', 'Rückfragen ans Unternehmen'). Deterministic, no LLM call, no new files needed. Keep manual day/step editing as the secondary 'anpassen' path it already is. — *The surface goes from a chore (build your own curriculum from nothing — the exact reason it was hidden) to a 5-second commitment: 'my interview is on the 12th' → a complete, dated battle plan appears. This is the one thing no drill in the app does — anchor training to a REAL hiring event — which is the mission stated literally: get the learner HIRED.*
- **TODAY-first layout: giant days-left hero, today's step auto-open, one orange CTA** (M): Rework PlanView: (1) hero header with the days-left number huge in blue (#3b82f6) — 'NOCH 5 TAGE' — over the existing progress bar; (2) a pinned HEUTE card: the DayCard matching todayStr() renders first with its first undone StepRunner's `open` state defaulted to true and one orange (#f97316) full-width button ('HEUTE ÜBEN' → opens/starts that step, min-height 48px); (3) past days collapse to one-line done/missed strips with a one-tap 'auf heute verschieben' for missed steps (pure cloneDays()+putPlan, already exists); future days collapse to compact rows. In the same pass, premium ergonomics: raise every sub-10px font to ≥11px, give the 20px done-toggle a 44px hit area via padding, give xBtn/smallBtn minHeight 44 (or 44px invisible padding), and replace the off-palette error reds (#f87171, #fca5a5, rgba(239,68,68)) with neutral slate text + '⚠' prefix so the file passes the 2-color law without making orange mean 'error'. — *Opening the plan stops feeling like reading a spreadsheet and starts feeling like a countdown ritual: the learner sees how many days stand between them and the job, and the single next action is one thumb-tap away — daily return behavior instead of one-time setup. The ergonomics pass makes it usable on the phone where the learner actually trains.*
- **Re-enter home as a live countdown chip (answers the 'clutter' verdict, not a revert)** (S): In App.jsx, replace the disabled generic button (line ~4410) with a conditional chip: on home load, GET /api/plans (token already in scope); if an unfinished plan with a future deadline exists, render a single 44px-tall chip — blue border, orange days number — 'INTERVIEW IN 5 TAGEN · Heute: Sprechen 🗣' → setZielplanOpen(true); if the nearest plan is fully done or overdue, show its state ('✓ Plan abgeschlossen' / 'überfällig'); if NO plan exists, render only one quiet ghost-text line 'Echtes Interview geplant? → Countdown starten'. Reuse planStats logic (copy the small pure function or lift it). Add an OWNER-AR slot for the chip's Arabic label (do not author it). Keep the Brain 'apply' route (line 4265) as-is. — *Fixes the fatal flaw that the whole surface is currently unreachable for normal users, while honoring the strategy-audit objection: it no longer forks the 'what do I open today' decision — when a real interview date exists, the chip IS the decision, and its live countdown ('in 5 Tagen') creates the urgency loop that brings the learner back every day until the real interview.*

## weekly-briefing
WeeklyBriefing.jsx (client/src/WeeklyBriefing.jsx) is the Elite flagship surface, but it renders Alhassan's briefing as one undifferentiated pre-wrap RTL text wall, even though the server contract (server/alhassan.js:318-324) guarantees EXACTLY four emoji-headed parts (🏆 what you beat, 📍 where you are, 🎯 the ONE move, 🔥 close). Biggest weakness: it is read-only and dead-ended — the briefing literally names the learner's ONE next move toward getting hired, yet offers no way to start it, shows the identical wall on every visit all week, and silently vanishes (state 'hidden') on any fetch error, so a paying Elite user can lose the feature with zero explanation.
- **Parse the 4-part contract into a real report, with an orange MISSION STARTEN button on the 🎯 part** (M): In WeeklyBriefing.jsx, split the text on the server-guaranteed headers — text.split(/(?=🏆|📍|🎯|🔥)/) — and render each segment as its own row: emoji as a left marker column, a tiny German label (🏆 DIESE WOCHE GESCHAFFT, 📍 DEIN STANDORT, 🎯 NÄCHSTE MISSION), body still dir=rtl, rows separated by 1px rgba(59,130,246,0.15) dividers. Wrap ONLY the 🎯 segment in an orange container (border 1px solid rgba(249,115,22,0.5), background rgba(249,115,22,0.08)) followed by a full-width 44px-min-height orange button '▶ MISSION STARTEN'. In App.jsx:4271 pass a new onAction prop that reuses the exact drill-open handlers already written at App.jsx:4245-4251 (setFluencyOpen/setShadowingOpen/setListeningOpen/setPressureOpen/beginSession); the component picks the drill by keyword-scanning the 🎯 text for the German/English drill names Alhassan uses (Hör-Check, Shadowing, Druck, Flow, fight, Interview) and falls back to beginSession() — the interview is always mission-true. If fewer than 2 segments parse, fall back to today's raw rendering, so nothing can break. No new Arabic authored; existing Arabic body text is untouched. — *The briefing stops being homework to decode and becomes the week's marching orders: the learner reads what they beat, sees where they stand, and taps ONE orange button to start the exact move Alhassan prescribed — the read-to-train loop closes in one gesture, which is the whole mission (get hired). Structurally it finally looks like a premium intelligence report instead of a pasted chat message.*
- **Turn it into a Monday ritual: NEU state on a new week, collapse-once-read, local season archive** (M): Keep the week value the API already returns (d.week — currently discarded at WeeklyBriefing.jsx:19) in state. Compare against localStorage 'wb_read_week': if different, render the card auto-expanded with a small orange 'NEU' pill (border 1px solid rgba(249,115,22,0.5)) pulsing via opacity; once the user collapses it (or taps a 'Gelesen ✓' row), write the week to localStorage and from then on render only a single 44px tappable header row — '📋 WÖCHENTLICHES BRIEFING · ELITE ▸' — that expands on tap. On every successful fetch, also push {week, text} into a localStorage 'wb_archive' array (cap 8) and show a dim footer toggle 'Frühere Wochen (n)' (Arabic twin label: OWNER-AR) that lists past briefings in the same parsed layout at reduced opacity. All inline styles, blue/orange/neutral only. — *The briefing becomes an appointment — one loud, fresh moment per week instead of a stale wall squatting on the home screen for seven days — and the archive turns isolated weekly notes into a visible season of progress ('look how far since week 1'), which is exactly the sustained-momentum story that keeps a candidate training until they are hire-ready. Also reclaims home-screen calm on every non-Monday visit.*
- **Never vanish silently: honest error + retry, and a premium skeleton while Alhassan writes** (S): In WeeklyBriefing.jsx replace the silent setState('hidden') on !r.ok / catch (lines 17 and 20) with: hidden ONLY for 403 elite_only, otherwise state 'error' rendering the same card frame with 'Dein Briefing ist gerade nicht erreichbar.' (Arabic twin line: OWNER-AR) plus a full-width 44px retry button (transparent background, 1px solid rgba(59,130,246,0.5), blue text) that re-runs the fetch — pull the fetch body into a loadBriefing() function so retry is one call. Upgrade the loading state (line 30) from one dim line to three rounded skeleton bars (heights 12px, widths 90/70/80%, background rgba(148,163,184,0.15)) under a German status line 'Alhassan schreibt dein Briefing…' — the existing Arabic loading line stays as-is (already authored). Matters doubly because the first uncached call of the week runs a live Groq generation and takes seconds. — *The flagship Elite feature can currently disappear without a trace on any 502/503 — to a paying user that reads as 'I paid and got nothing.' With an honest error card and one-tap retry it reads instead as a live service momentarily busy, and the skeleton makes the weekly generation wait feel like a report being written for them rather than a hung UI. Cheapest possible move for the biggest trust gain.*

## mentor-chat-alhassan
Alhassan is the app's soul on the server (persistent memory, real learner facts, brain-directed next moves) rendered through a bare, off-system chat shell on the client: replies pop in as full walls of text after a dead-silent round trip, the skin is a leftover green/cyan gradient that violates the blue+orange law (Alhassan.jsx lines 85, 93-94, 104), and — the biggest weakness — every reply ends on a concrete next move (server/alhassan.js IRON RULE 3 + the brain directive) that the learner cannot tap; the mentor points, the UI shrugs.
- **Tappable next-move chips — the mentor's finger becomes a button** (M): Add a client-side detectAction(text) in Alhassan.jsx: a deterministic regex over each assistant reply for the known action vocabulary the server already emits (Live-Interview/فايت, WIEDERHOLUNG/المراجعة, SHADOWING, SAG-ES-RICHTIG, FLOW-DRILL, HÖR-CHECK, DRUCK-LEITER, Einstufung). When one matches, render ONE orange action chip (background rgba(249,115,22,.12), border #f97316, minHeight 44px, full-width on mobile) under that bubble. Wire it via a new prop from App.jsx: <Alhassan onAction={(id)=>{ setGuideOpen(false); /* map id → existing openers: start interview, openDashboard/SRS, setListeningOpen, setPressureOpen, etc. */ }}> at App.jsx:3617-3619. Orange stays the single action accent; everything else in the chat stays blue/neutral. — *This is the mission in one tap: Alhassan says "روح خلّص الـ N مراجعات" and the learner is IN the review two seconds later instead of closing the chat and hunting the home wall. The brain→mentor→action loop closes, drill completion after chat sessions goes up, and the chat stops being a place where momentum goes to die.*
- **Alive replies: progressive reveal + breathing typing indicator** (S): Keep the existing non-streaming fetch but reveal the received reply progressively in Alhassan.jsx: store the full text, then a ~35ms setInterval appends 1-2 words per tick to the visible bubble (auto-scroll already follows via the existing effect at line 30). Replace the static '…بيكتب' (line 71) with three blue (#3b82f6) dots pulsing via the app's existing global keyframes (or a JS opacity tick — no new CSS file needed), inside a proper assistant bubble with the avatar. Add a 0.15s translateY entrance on new bubbles reusing the existing flash-in pattern. — *The single biggest premium tell in any chat is whether the mentor feels like he is THERE. Today the learner stares at a frozen ellipsis for 2-4s and then a wall of Arabic slams in — it reads as a form submission, not a person. Word-by-word arrival makes the same Groq latency feel like Alhassan thinking and talking, at $0 and zero server changes.*
- **On-law reskin + identity header + starter chips (kill the green ghost)** (M): Rebuild the shell in Alhassan.jsx to the design law: overlay radial in navy neutrals (e.g. #0b1220→#020617) replacing the green #0c1a14/#060c0a (lines 93-94); send button solid #f97316 with white text (replacing dark-green #04130c, line 85 — send IS the action, so it earns the orange); user bubbles neutral slate text (#e2e8f0) not cyan #cffafe (line 104); assistant bubbles stay blue-tinted. Add a real header: 40px blue circle avatar with 'ح' monogram + name + the existing '24/7' line. When history is empty or on reopen, show 2-3 tappable 44px starter chips that send preset questions — German copy I can write ('Wie war mein letztes Interview?', 'Was ist mein nächster Schritt?'), Arabic chip labels marked OWNER-AR for the owner to author. Bonus polish in the same pass: per-message dir auto-detect (Arabic chars → rtl, else ltr) so German tutoring answers render correctly. — *Right now the app's warmest human feature wears the app's ugliest leftover skin — a green/cyan theme design-lint should be failing, which silently tells the learner 'this corner is unfinished.' A navy/blue/orange shell with a face and instant starter chips makes opening Alhassan feel like opening a premium coach, and the chips kill blank-canvas paralysis at the exact moment (post-loss debrief, App.jsx:1923) the learner is most lost.*

## daily-mission
DailyMission.jsx is the right spine — a deterministic readiness ring, a warm coach line, the named #1 weakness, and ONE routed mission — and DailyTraining.jsx is a legit recall engine (retype gate, generation cue, streak shield). But the surface is a snapshot, not a loop: the score shows no movement, tapping the mission produces zero acknowledgment afterward, readiness never points past drilling toward actually APPLYING, and the panel's own container breaks the 2-color law with a green+cyan gradient (DailyMission.jsx:118-119) while DailyTraining still speaks through the retired lara voice on the minute-gated /api/tts (DailyTraining.jsx:56) with ~20px speak buttons (speakBtnSt, line 359).
- **Make the ring ALIVE: weekly delta + the 75-point Bewerbungs-Schwelle that ends in an application** (M): In DailyMission.jsx computeReadiness(), also compute the PREVIOUS score from trends slices (-10..-5) vs (-5..) and render a blue '+N diese Woche ▲ — Füssigkeit trieb es' chip beside the ring (attribute the delta to whichever sub-score moved most; hide chip when <10 sessions — never fake a trend). Draw a tick on the conic ring at 75 labeled 'BEWERBUNGS-SCHWELLE' (a 2px neutral notch via a second conic-gradient layer). When score >= 75, nextMission() returns drill:'apply' ('Du bist über der Schwelle — bewirb dich diese Woche', AR slot = OWNER-AR) and App.jsx:4245 onOpen gains `else if (drill === 'apply') setZielplanOpen(true);` — the Zielplan overlay already exists at App.jsx:3530. While touching the panel, replace the illegal green/cyan gradient + cyan border (lines 118-119) with rgba(59,130,246,…) tones, and keep orange only on the single mission CTA. — *The score stops being a static number and becomes visible momentum ('I moved +6 this week because of X'), and for the first time the home surface has an EXIT toward the actual mission — hired — instead of an infinite drill treadmill. It also makes the flagship panel pass the design lint it currently fails.*
- **Close the loop: mission-complete state the moment you return from the drill** (M): In App.jsx add `const [homeRefresh, setHomeRefresh] = useState(0)` and bump it in the onClose of the five overlays the mission routes to (fluency/spoken/pressure/listening + fight debrief exit); pass it as a `refreshKey` prop to DailyMission (mounted at App.jsx:4243) and add it to the fetch useEffect deps (DailyMission.jsx:94-105) so the panel refetches /api/progress. When the refetched data shows d.trainedToday (or dueReviews dropped / sessions incremented vs the previous fetch kept in a ref), swap the orange CTA block for a calm blue '✓ MISSION ERFÜLLT' band + 'Morgen: <nextMission preview>' + a 7-dot streak strip (filled dots = min(streak,7), blue; today's dot orange). German copy authored; every Arabic string is an OWNER-AR slot. — *Right now the learner drills, comes back, and the home screen acts like nothing happened — the single cheapest premium signal is an app that visibly REMEMBERS what you just did. Completion + tomorrow-preview + the dot chain converts one mission into a chain (loss aversion the surface already believes in but never renders here).*
- **Speak-first Daily Training on the one native voice (plus legal touch targets)** (M): In DailyTraining.jsx: (1) replace the speakCard fetch to /api/tts with the shared helper from client/src/nativeVoice.js (/tts-stream?drill=1) so it uses the app's one native Aura-2 voice (elara — lara at line 56 was explicitly retired by the owner) and skips the interview-minute gate; keep the SpeechSynthesis fallback. (2) On a wrong answer, auto-play result.expected the instant the Lösung renders (call speakCard from the submit() failure branch) so hear+retype happen together; after retypeOk, insert an UNGRADED '🗣 Sag es 2× laut' beat (replay button + a 'GESAGT ✓ WEITER' confirm) before next() — no browser speech grading, so it honors both the removed-shadow decision and the feedback-accuracy doctrine. (3) Completion screen adds one deterministic line from data already on the client: 'X deiner Fehler geschlossen · Y Fragen richtig' (count q.source==='mistake' answered correct). (4) Bump speakBtnSt and cueBtn to minWidth/minHeight 44. Arabic strings = OWNER-AR slots. — *The daily habit finally trains the thing the job tests — the mouth, not the fingers — at $0 and with zero fake grading; the voice matches every other drill instead of the one the owner rejected; and thumbs can actually hit the audio buttons on a phone. The completion line ties the 3 minutes directly to 'errors toward hired closed today'.*

## invite-referral
InviteCard.jsx is a 35-line, Arabic-only coupon card ("send link, both get +3 days") buried at position ~8 of the idle home (App.jsx:4278), with a blue-gradient button that breaks the orange-single-action law (near-black #04110b text, ~40px height under the 44px touch floor). Its biggest weakness: it is a dead-end — after tapping share NOTHING ever changes (no sent state, no pending state, no earned-days readout even though the server credits bonusTrialDays), and the share fires at the lowest-emotion moment with a generic line, so the loop never breathes.
- **Reframe as TRAININGSPARTNER + make the card design-lawful** (S): In client/src/InviteCard.jsx: (1) rebrand from coupon to mission — header becomes DE-first 'TRAININGSPARTNER HOLEN' with the existing masri line kept verbatim underneath (any NEW Arabic = OWNER-AR slot); add one German sub-line ('Zu zweit trainiert man härter — ihr bekommt beide +3 Tage.') and a tiny two-seat visual: two inline 44px rounded 'seats' side by side (pure divs, blue #3b82f6 border, orange '+3 TAGE' badge on each) showing DU + DEIN FREUND both winning. (2) Fix the CTA to the ONE lawful action accent: solid #f97316 background, #fff text, fontWeight 800, minHeight 44, borderRadius 10 — delete the blue gradient and the #04110b text. (3) Add a visible link pill above the button (monospace, fontSize 11, color #cbd5e1 on rgba(59,130,246,0.08), text-overflow ellipsis, tap-to-copy) so the user sees exactly what will be sent. Run check-design + check-german after. — *The learner stops seeing a generic 'free days' coupon and sees the mission: recruit a sparring partner for the scariest thing they will ever do in German. The orange CTA finally reads as THE action (today it is a rule-breaking blue blob), the visible link builds trust before sharing, and 44px means it actually works on the phones this audience uses.*
- **Close the loop: an honest idle → gesendet lifecycle instead of fire-and-forget** (M): Pure client state machine in InviteCard.jsx, $0 and honest (no fabricated 'friend joined'): on successful navigator.share resolve or clipboard copy, persist {count, lastTs} to localStorage key 'bpo_invites_sent'. When count > 0 the card renders a 2-step progress rail in blue (step 1 filled: 'Link gesendet ✓' + honest local counter 'N Einladungen gesendet'; step 2 hollow: 'Freund macht sein 1. Interview → +3 Tage für euch beide' — Arabic mirror line = OWNER-AR slot) plus a secondary ghost button 'Noch jemanden einladen' (border 1px solid #3b82f6, transparent bg, minHeight 44) so multi-invite is invited, not accidental. Everything shown is locally true — 'sent' means sent, never 'joined'. Follow-up (out of this JSX-only scope, flag for owner): one server line adding bonusTrialDays to publicAccount() in server/auth.js would light up step 2 as '+3 Tage gutgeschrieben' for real. — *Today the button gives 1.8s of feedback then amnesia — the user has no evidence anything happened and no reason to ever touch the card again. A card that visibly remembers 'you invited 2 friends, reward pending' feels alive, teaches HOW the reward triggers (friend must finish an interview — the #1 reason referral loops silently die), and turns one share into several.*
- **Fire the invite at the WIN moment: post-debrief challenge variant** (M): Give InviteCard an optional prop ({ variant: 'challenge', minutes, bossName }) and render a compact second instance in App.jsx directly under the interview debrief (near the existing debrief block), only when a session actually completed. The share text weaves in REAL client-side session facts — nothing fabricated: German line built from actual data, e.g. `Ich habe gerade ${minutes} Minuten Bewerbungsgespräch auf Deutsch mit ${bossName} durchgestanden. Schaffst du das? Wir bekommen beide +3 Tage:` + link; keep the existing masri sentence verbatim as the second line, new Arabic challenge phrasing = OWNER-AR slot. Home instance stays as the calm evergreen entry; the challenge variant reuses the same orange CTA and writes to the same localStorage counter from move 2. — *This is the sharpest growth lever on the surface: invites convert when sent at peak pride, and 'I just survived a German interview — can you?' recruits exactly the right people (fellow candidates who will train and get hired too, which IS the mission). It also makes the app feel alive — it reacts to what the learner just achieved with a true, personal brag instead of a static coupon on the home screen.*

## assessment-einstufung
The free Einstufung is the app's conversion hook: 5 recorded German questions, cheap Groq transcription, one analysis call, then a verdict that Assessment.jsx's own header admits is a "Phase-1 simple render" awaiting the "polished arena results screen". Its biggest weakness is the payoff moment: a flat grade card (~A2 in a big number, red-tinted blocker list that breaks the blue+orange law) that never shows the learner their distance to HIREABLE, never links a blocker to the drill that fixes it, and hands off to an interview that ignores the measured level — plus the core 5x60s recording interaction is a dead kitchen timer (ClipRecorder's onVolume is wired to a no-op at line 63).
- **Verdict becomes 'Der Weg zur Einstellung' — a hire-gap rail with tap-to-train orders** (M): Rebuild Verdict() in client/src/Assessment.jsx (the promised Phase 2). (1) Above the level number, render an A1-A2-B1-B2-C1 rail as 5 inline-flex segments: blue #3b82f6 fill up to estimatedLevel, neutral rgba-white beyond, and an orange #f97316 marker+label at B2 reading 'AB HIER: TELEFON-JOB' (Arabic label = OWNER-AR slot). Headline reframes the grade as distance: 'Noch 2 Stufen bis einstellbar' computed from the level index — keep the existing honesty disclaimer untouched. (2) Restyle blockers from red #f87171/#fca5a5 (off-palette, design-law violation) to orange as 'DEINE AUFTRAEGE', each card getting a 44px 'Jetzt trainieren' button. (3) Wire the buttons via a new onOpenDrill prop passed from App.jsx line ~3578, mapped to the existing OPEN drill-setter map (App.jsx ~4262); pick the drill client-side with a deterministic keyword regex on blocker.rule (verbstellung|satzbau|nebensatz -> Sag-es-Richtig; hoer|verstehen -> Hoer-Check; aussprache -> Shadowing; wortschatz|fluessig -> Flow-Drill; default -> interview). All inline styles, zero deps, zero server changes. — *The single highest-leverage screen in the funnel stops being a school grade and becomes a hiring roadmap: the learner sees exactly how far they are from a German phone job and taps straight from their #1 weakness into the drill that trains it — mission-true (HIRED, not certified), premium (the placeholder finally ships), and it fixes a live design-law breach.*
- **Make the 60-second take feel alive — live level meter + silence sentinel that saves the one-shot answer** (S): In Assessment.jsx only: (1) pass a real onVolume callback into ClipRecorder (line 63 currently discards it — the plumbing already exists end-to-end through audioRecorder.js) and drive a 12-bar blue level meter with a ref + requestAnimationFrame transform:scaleY (same volRef pattern App.jsx's WaveformRing already uses, so no re-render per frame). (2) Silence sentinel: if cumulative volume stays near zero for the first ~5s of a take, show an inline warning while STILL recording — 'Wir hoeren nichts — sprich lauter oder pruefe dein Mikrofon' (Arabic = OWNER-AR slot) — because today a muted mic is only discovered after upload (server noSpeech -> '(nichts erkannt)'), which can burn the learner's single re-record. (3) Swap the red #ef4444 stop button to the orange action color and let the last-10s countdown pulse orange (it already switches to var(--action)). All inline-style, 44px targets preserved. — *Recording five answers IS the assessment experience, and right now it looks dead — a static timer on a dark screen. A meter that breathes with their voice reads as premium and confirms 'the app hears me' (huge for first-session trust on Egyptian mobile mics), and the sentinel prevents the worst possible first impression: wasting the once-per-account assessment on a muted microphone.*
- **Close the two dead-ends: the verdict actually sets the interview level, and the wait/return states work for the mission** (S): (1) Seed the arena from the result: in App.jsx line 3580, change onStartInterview to receive the level from Verdict and call the existing chooseLevel(['B2','C1'].includes(lvl) ? 'b2' : 'a2-b1') before beginSession() — today the measured level is thrown away and everyone starts at the default 'a2-b1'. (2) Replace the bare 'analyzing' spinner in Assessment.jsx with an honest 'Worauf der Pruefer achtet' panel: the 4 real rubric criteria the model judges (Verbstellung, Wortschatz, Fluessigkeit, Struktur) as static blue-tinted rows plus a count of usable vs empty answers — teaching the rubric during the wait, with NO fake progress animation (owner's no-fake-metrics law). (3) On the stored-verdict return path (status.used), use result.at + the 28-day paid cadence to render either 'Neu-Einstufung in X Tagen' for paid users or an orange one-liner + onGoPricing hook for free users ('Neu einstufen = Elite'), turning the one-per-account wall into a visible, honest upgrade reason. Arabic for new lines = OWNER-AR slots. — *The assessment finally CONTROLS the product instead of being a detached quiz: a B2 candidate lands in a B2 interview seconds after their verdict (personalization the learner can feel), the dead 10-second analysis wait becomes a micro-lesson in what German examiners listen for, and the re-test cadence gives the owner a zero-cost, non-fake conversion lever on every returning free user.*

## paywall-pricing
A functional bilingual EGP paywall (plan cards → monthly/yearly toggle → Vodafone Cash manual flow with reference code → static "we're verifying" state) plus the PlacementPrompt hired-survey card. Its biggest weakness: it is generic and inert — the `info` entitlement prop passed into PaywallScreen is never read, so a learner whose trial just ended, whose daily minutes are honestly used up, or who tapped a locked drill all see the same wall selling "minutes" instead of the hiring mission; the post-payment 30-minute wait is a dead black box; and this surface carries hard-rule breaches (employer names "Majorel, Teleperformance" in PlacementPrompt's placeholder, green/sky hues, Share Tech Mono, sub-44px chips).
- **The wall that knows you — context-aware reason strip from the unused `info` prop** (M): In client/src/App.jsx, tag every paywall launch with a source: change the 5 drill call sites (~3587–3626) and beginSession (3393) to setPaywall({ ...auth.account?.entitlement, source: 'shadowing' | 'fluency' | 'listening' | 'spoken_review' | 'trainingslager' | 'interview' }); the WS S.PAYWALL case (2851) already carries the entitlement. Inside PaywallScreen, finally READ `info` and render a reason strip above the plan cards (blue #3b82f6 1px-border card, key phrase in orange #f97316): trial ended → 'Deine Probezeit ist vorbei — dein ganzer Fortschritt bleibt gespeichert.'; paid minutes spent → positive framing 'Deine X Min für heute sind trainiert ✓ — morgen gibt es neue. Mit ELITE wären es 15 Min/Tag.'; free fight spent → 'Dein kostenloses Interview ist absolviert.'; source=drill → 'Shadowing gehört zum bezahlten Training.'. Trial still active → thin blue progress bar built from info.trial.daysLeft (pure divs, inline styles). Author DE only; every Arabic line renders the DE fallback behind an OWNER-AR marker constant until the owner supplies copy. — *The single highest-stakes screen stops feeling like a slammed door and starts feeling like a coach who knows exactly where the learner stands — the difference between 'pay up' and 'here is your next step'; it is also the cheapest honest conversion lift available since all data already arrives in the prop.*
- **Sell the mission, not minutes — funnel-stage plan cards with honest per-day math** (S): In PaywallScreen's plan-cards view (App.jsx ~2467–2547): (1) add one mission line under the header: 'Ein Ziel: eingestellt werden.' (orange, --font-display); (2) re-head each card with its hiring-funnel role via a small stage label above the plan name — BASIC → 'JEDEN TAG interviewfähig' (blue), ELITE → 'Bis zur Einstellung: 2 volle Interviews pro Tag' (orange), and rename the elite badge from 'Beliebt für Interview-Prep' to the mission-true 'Für die Interview-Phase'; (3) under the price render real arithmetic, no fake metrics: `≈ ${fmt(Math.round(price / (yearly ? 365 : 30)))} EGP/Tag` in neutral #94a3b8 — an anchor that makes a monthly EGP number feel like a daily coffee decision; (4) color the perk checkmarks — blue ✓ for shared perks, orange ✓ for elite-only ones — so the upgrade delta is visible in half a second; (5) add one trust line beneath the cards: 'Deine Fehleranalyse und dein Fortschritt bleiben immer erhalten.' Arabic equivalents = OWNER-AR slots. Only #3b82f6 / #f97316 / neutrals; kill the Share Tech Mono on the Vodafone number (line 2398) → var(--font-display). — *Learners stop comparing 'minutes/day' like a phone plan and start seeing the price of getting hired — the owner's mission becomes the sales pitch, honestly, with zero invented numbers (the per-day figure is pure division of the real price).*
- **Kill the post-payment black box — live activation watcher + hard-rule sweep of PlacementPrompt** (M): Part A (App.jsx PaywallScreen): while the pendingPayment view (2444–2465) is mounted, add a useEffect that polls /api/billing/status every 45s; the moment the plan flips active (pendingPayment gone + paid plan), call onUpgraded(d.account) — the existing handleUpgraded + justActivated celebration take over instantly, no app reopen. Visual life: a pulsing blue dot next to 'Wir prüfen deine Zahlung' (reuse the existing flash-in keyframe pattern or a 2-frame opacity animation defined once) and a secondary 44px button 'Weiter trainieren — die Einstufung ist immer gratis' that closes the wall so the wait is never dead time. Part B (client/src/PlacementPrompt.jsx, same surface, same pass): DELETE the employer names in the placeholder (line 100, hard no-employer-names rule) → 'z. B. dein neuer Arbeitgeber' (AR slot = OWNER-AR); recolor the 'hired' chip from green rgba(34,197,94)/#86efac to the orange action accent (getting hired IS the mission moment — it deserves the one accent) and all sky #0ea5e9/#e0f2fe/rgba(56,189,248) to blue #3b82f6 tints + neutrals; bump every stage chip and the Speichern button to minHeight 44. — *The scariest moment in the product — real money sent into a manual 30-minute void — becomes a living, trustworthy screen that celebrates the instant the owner activates; and the surface passes the owner's own design-lint and no-employer-names law instead of quietly violating both.*

## brain-guide-journey
BrainGuide.jsx is a single-fetch RTL card (live NOW — BRAIN_GUIDE_LIVE=true at App.jsx:61) showing a numeric journey fraction, an anonymous 8px progress bar, and one CTA. Its biggest weakness: it discards most of what GET /api/brain already sends (directive.state, confidence, target.skillId, tier, plus top-level hireReady/hireNote), fetches exactly once on mount so it goes STALE after every drill/interview the user completes, and its CTA is a blue gradient with a stray #04110b text color even though orange is the design system's single action accent.
- **Turn the bare bar into a named hiring roadmap that ends at BEWERBEN** (M): In BrainGuide.jsx replace the 8px track (lines 78-81) with a milestone path built purely from data already in the payload: render journey.entryTotal small nodes on the track — mastered nodes solid blue #3b82f6, the ONE next node orange #f97316 with the app's existing global 'pulse' keyframe, remaining nodes hollow neutral (rgba(255,255,255,0.15) border) — capped by a labeled destination node 'BEWERBEN' (flips fully orange when directive.state==='APPLY' / tier.applyNow, using the currently-unused data.hireReady + hireNote). Add a client-side SKILL_LABEL_DE map for the ~17 skill ids in server/brain/skillGraph.js (self-intro, sie-register, word-order-sub, listen-phone, konjunktiv-2, deescalate, …) and render 'Nächster Baustein: {label}' from d.target.skillId above the CTA (German copy is allowed — gate with check-german; add a null journeyGoalAr slot in BRAIN_COPY marked OWNER-AR, rendered only when authored). — *The learner stops seeing an abstract percentage and starts seeing THEMSELVES standing one named node away from a visible job-application finish line — the mission (get HIRED) becomes the picture on the home screen, and each drill is framed as unlocking a named skill on that road.*
- **Make the card ALIVE: refetch when the learner returns from a drill + animate the earned delta** (S): BrainGuide fetches once on mount (useEffect deps [token, apiUrl]) while drills open as overlays with the guide still mounted — so after finishing the prescribed drill the card silently shows yesterday's prescription. Add a refreshKey prop to BrainGuide and include it in the effect deps; in App.jsx (line ~4257) pass a value that changes whenever the home returns to idle, e.g. refreshKey={String([shadowingOpen, spokenReviewOpen, fluencyOpen, listeningOpen, pressureOpen, dailyOpen, assessmentOpen, phase])}, plus a document 'visibilitychange' listener inside the component for mobile app-switch returns. Keep the previous journey.pctToApply in a useRef; when fresh data arrives higher, set the fill to the old width first then the new width on the next frame — the existing transition:'width .4s' (line 92) tweens it live — and show a transient (~4s setTimeout) orange chip '+1 Schritt' next to the bar, with a null stepUpAr BRAIN_COPY slot marked OWNER-AR for the celebratory line. — *The learning loop physically closes in front of the learner: finish the drill, come back, WATCH your road fill one node — the single strongest 'this app is alive and it noticed me' moment, and it fixes a real staleness bug (stale prescription = the brain looks dumb right after you obeyed it).*
- **Show the engine's receipts: evidence line, before→after aha visual, and the orange action CTA** (S): Three edits in BrainGuide.jsx. (1) WHY line above the CTA from fields already sent but dropped: confidence==='high' (engine only says this with ≥2 sessions of evidence, engine.js:97-99) → 'Gemessen in deinen letzten Interviews: {SKILL_LABEL_DE[target.skillId]}'; confidence==='low' → 'Erste Einschätzung — wir messen weiter'; state==='PLATEAU' and 'MEASURE' get their own one-liners (all German, check-german gated; parallel null OWNER-AR slots in BRAIN_COPY). (2) Upgrade the aha box (lines 70-75) into a mini before→after visual using the true d.aha.before/after counts: two thin inline-div bars, 'before' neutral rgba-white with the count, 'after' blue #3b82f6 — no new claims, just the engine's confirmed closed loop made visible. (3) Design-law fix: the CTA (line 94) is a blue gradient with color '#04110b' (a non-neutral stray) — make it solid var(--action) #f97316, color '#fff', minHeight 48 (explicit ≥44px touch target); the roadmap stays blue so orange remains the single action on the card. — *The card stops reading as a generic banner and reads as a coach who shows receipts — 'measured in YOUR last interviews' plus an honest visualized win builds the trust that makes a $0 app feel premium, and it enforces the feedback-accuracy doctrine (never overclaim on thin evidence) at the UI layer.*

## onboarding-first-run
Onboarding today is a signup-first auth screen of static bilingual text promises (client/src/App.jsx:2148-2257), an auto-opened free placement after signup (App.jsx:2687-2696), and a one-shot dismissible SO-FUNKTIONIERT'S text card above the mic controls (App.jsx:4093-4107). Its biggest weakness: nothing is alive or guided — the product is described instead of shown, the placement result is displayed then thrown away (the first interview runs at the default 'a2-b1' level regardless of the verdict), and the only "tutorial" is three lines of text the mic-permission ambush can still wreck.
- **Make the placement actually calibrate the first interview (close the broken seam)** (S): Assessment.jsx:308 currently calls onStartInterview() with nothing; App.jsx:3580 then runs beginSession() with levelRef still at its 'a2-b1' default (App.jsx:2701). Fix: pass result.estimatedLevel up (onStartInterview(result.estimatedLevel)), and in the App.jsx:3580 handler map it to the existing level ids ({A1:'a2-b1',A2:'a2-b1',B1:'a2-b1',B2:'b2',C1:'c1'}) and call chooseLevel(mapped) before beginSession(). On the Verdict button (Assessment.jsx:309) change the label to 'Erstes Interview starten — Niveau ~B2' using the same mapping so the learner SEES the calibration happen (AR label slot: OWNER-AR). Pure JSX edits, two files, zero new strings of German logic. — *The app's core credibility moment. A learner placed B2 who then meets an A2 interviewer concludes the placement was theater; a learner placed A2 thrown at C1 formality freezes and quits. Honoring the verdict makes the very first fight pedagogically correct (hired-mission-true) and makes the free placement feel like it MATTERED — the strongest possible first-session retention hook, for one afternoon of wiring.*
- **Auth screen: show the interviewer, don't describe him (living proof above the fold)** (M): Reuse the existing BossAvatar component (already in App.jsx, used at line 3990) inside AuthScreen: a ~110px avatar between the tagline and the proof bullets, breathing via the existing 'breathe' class, with speaking={true} pulsed on a 5s interval while a typewriter line (setInterval slicing a string into state — no deps) types a real opening question: 'Erzählen Sie mir von sich.' then cycles 2-3 authentic HR questions, blue accent only. Caption under it: 'Das ist dein Interviewer. Er wartet.' + one OWNER-AR slot. While in the file, fix the touch-target violations: the login/signup mode tabs (App.jsx:2222-2229, padding '8px' ≈34px tall) get minHeight:44, fontSize 11; keep orange OFF this screen except the single 🎯 promise line so KONTO ERSTELLEN reads as the one action. — *Cold link-clickers currently must trade an email for a wall of claims. Seeing the actual boss avatar animate and ask a real German interview question is proof-of-product in 3 seconds — the moat ('live voice interview vs. a hard HR boss') demonstrated, not asserted. It converts the auth screen from a form into a threshold: the interviewer is literally waiting on the other side of the button. All from JSX already in the same file, $0, design-system-clean.*
- **Replace the SO-FUNKTIONIERT'S blurb with a live 3-step first-mission checklist (incl. mic preflight)** (M): Swap the static card at App.jsx:4093-4107 for a stateful FirstMission checklist shown while rank?.sessions === 0 (server truth from /api/progress, App.jsx:3453 — not just localStorage). Three 44px tap rows, auto-checking: (1) EINSTUFUNG — ✓ if /api/assessment/status.used (endpoint already called at App.jsx:2694), else taps setAssessmentOpen(true); (2) MIKROFON TESTEN — taps getUserMedia, shows a 2s live level via the existing volRef/WaveformRing metering pattern, ✓ on signal, and on failure surfaces the existing mic_denied copy (App.jsx:88) INLINE with the lock-icon instructions; (3) ERSTES INTERVIEW — the single orange element on the idle home, calls beginSession(). Card celebrates each ✓ (blue flash, reuse screenFlash pattern) and disappears forever once sessions>0. Keep existing DE strings, mark every new Arabic line OWNER-AR. Same PR: replace the off-system cyan rgba(0,188,212,…) in the pre-fight briefing card (App.jsx:3896,3910-3920) and the '#67e8f9' SENDEN gradient (App.jsx:4157) with the blue tokens so design-lint stays green. — *The #1 silent first-run killer is the mic permission prompt firing mid-interview — the learner's first live German conversation dies to a browser dialog, and the current text card can't prevent it. A checklist that pre-flights the mic, reflects REAL state (placement done, mic proven, interview next), and reserves orange for exactly one next action turns onboarding from a paragraph into a mission — the learner's first 5 minutes feel engineered toward getting hired, and the owner gets a deterministic, $0, fully client-side funnel with zero new failure modes.*

## trainingsnachweis-pdf
A 90-line modal certificate (client/src/Trainingsnachweis.jsx) showing a 6-stat vanity grid (sessions, streak, vocab count) headed by the learner's raw email and an emoji logo, with a window.print() button. Its biggest weakness is fatal: the print CSS at line 17 (`body > *:not(#nachweis-root){display:none}`) hides `#root` — an ancestor of the cert since there is no portal — so the one promised feature, "Fortschritt als PDF drucken", outputs a BLANK page. Meanwhile the server already computes exactly what a hiring document needs (CEFR hireReadiness with honesty notes, fluency trends, training date range in server/progress.js) and none of it reaches this surface.
- **Fix the blank-page print bug and make the printout a real A4 document** (S): In Trainingsnachweis.jsx replace the broken `body > *:not(#nachweis-root){display:none}` rule with the ancestor-safe visibility technique: `@media print { body * { visibility: hidden !important; } #nachweis-root, #nachweis-root * { visibility: visible !important; } #nachweis-root { position: absolute !important; inset: 0 !important; } }` plus `@page { margin: 16mm; }`. Replace the 🥊 emoji logo (line 39) with a small inline-SVG 'OP' monogram in a blue #3b82f6 stroked circle (renders identically on screen and on b/w printers; emoji rasterize unpredictably in print), give the 🖨 button plain text 'DRUCKEN / PDF SPEICHERN', and add print overrides so title/divider ink is #000 with text-shadows stripped. Pure CSS/JSX edits in the existing dangerouslySetInnerHTML block — no deps, no new hues. — *Today the learner taps DRUCKEN and gets an empty page — the surface's entire reason to exist silently fails, and every learner who tried it concluded the app is broken. After this, the button produces a clean black-on-white A4 sheet they can actually save and send with an application.*
- **Turn the stat-dump into hiring evidence: the Sprachniveau-Dossier** (M): App.jsx already fetches /api/progress (lines 3442-3457) — store two more fields it currently drops (`data.hireReadiness`, `data.trends`) and pass them at the render site (lines 3548-3554). Rebuild the cert hierarchy: (1) headline = big CEFR level from hireReadiness.level ('Gesprächsniveau: B1') with the server's honest footnote verbatim ('Vorläufige Einschätzung — X/9 Signale gemessen'), never printing a hireReady claim when the server returns null; (2) 'Trainingszeitraum: <first trends.dates> – heute · N Live-Interviews unter Echtzeit-Druck' — the differentiator no competitor can print; (3) 'Aktueller Trainingsfokus: <limitingSkill>' mapped to professional German labels (Flüssigkeit/Grammatik/Verständlichkeit...) — forward-framing, not shaming; (4) an editable 'Name für den Nachweis' field persisted to localStorage, shown as the document's addressee with the email demoted to a small identity line (Arabic label for this field = OWNER-AR slot, do not author). All existing colors, inline styles, 44px touch targets. — *An email-headed list of app points is unsendable; a named document stating a CEFR level (honestly caveated), a training period, and live-pressure interview count is the one artifact a candidate can hand a recruiter — this is the surface's direct line to the mission: getting HIRED.*
- **Make it alive: inline-SVG fluency curve + issued-document verification line** (M): Add a dependency-free inline <svg> sparkline of trends.fluency across the last 20 sessions inside the cert: a single blue #3b82f6 polyline with one orange #f97316 dot on the latest point, caption 'Flüssigkeit über N Sitzungen', print override to #000 stroke via a .nachweis-spark class. Honesty gate: render only when >=3 sessions with nonzero fluency exist, otherwise omit (no fake curve). Below the disclaimer, add an issued-document footer: 'Nachweis-Nr. XXXX-XXXX · erstellt am <date>' where the code is a deterministic pure-JS hash (djb2 over email+sessions+date, base36, ~15 lines, no dep) — recomputable later if the owner ever adds a /verify endpoint, zero server changes now. — *A snapshot number says 'where I am'; a rising curve says 'I am improving under pressure' — the strongest signal both the learner (motivation to keep training) and a screener (trajectory beats level) can feel in two seconds, and the Nachweis-Nr. makes the sheet feel issued by an institution instead of screenshotted from a game.*
