/**
 * Generated release manifest boundary. Intentionally empty.
 * A category may be added only from a finalized, participant-disjoint expert-gold
 * report that passes `evaluatePronunciationRelease` for that exact protocol/model.
 */
export const PRONUNCIATION_RELEASES = Object.freeze({});

